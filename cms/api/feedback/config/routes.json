{
  "routes": [
    {
      "method": "GET",
      "path": "/feedbacks",
      "handler": "feedback.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/feedbacks",
      "handler": "feedback.create",
      "config": {
        "policies": []
      }
    }
  ]
}
