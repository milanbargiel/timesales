{
  "routes": [
    {
      "method": "GET",
      "path": "/orders/:key",
      "handler": "order.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/orders",
      "handler": "order.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/orders/:key",
      "handler": "order.update",
      "config": {
        "policies": []
      }
    }
  ]
}
